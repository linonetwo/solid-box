import getTokens from '@kiwicom/orbit-components/lib/getTokens';

const customTokens = getTokens({
  palette: {
    product: {
      normal: '#7C4DFF',
      normalHover: '#7C4DFFEE',
      normalActive: '#7C4DFFBB',
    },
  },
});

export default customTokens;
